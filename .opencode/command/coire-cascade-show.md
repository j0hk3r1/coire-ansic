---
description: Show current bifrost routing rules with arena MT scores + quota classes. Optional --pool flag.
---

Use coire-cascade-show to dump current omo-* cascade with annotations.

Args (optional): $ARGUMENTS

Execute:
```bash
~/coire-tools/coire-cascade-show $ARGUMENTS
```

Show output verbatim. If user asked about a specific model, point to its row + position.
