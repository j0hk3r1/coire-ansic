---
description: Capture live bifrost config to repo snapshot + optionally commit. Pass --commit to add + commit.
---

Use coire-snapshot-sync to persist current bifrost state.

Args (optional --commit): $ARGUMENTS

```bash
~/coire-tools/coire-snapshot-sync $ARGUMENTS
```

Show output verbatim. If diff non-empty and no --commit, ask user if they want to commit before doing anything destructive.
